<form method="post" action="{{ path('app_ui_source_delete', {'id': source.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ source.id) }}">
    <button class="btn">Delete</button>
</form>
